services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: graceful-echo-app
    ports:
      - "8080"
      - "8443:8443"
    volumes:
      - ./certs:/app/certs:ro
